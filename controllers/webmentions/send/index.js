const send = require('./send-multiple') // same as sendMultiple
const sendMultiple = require('./send-multiple') // same as send
const sendSingle = require('./send-single')

module.exports = {
  send,
  sendMultiple,
  sendSingle
}
