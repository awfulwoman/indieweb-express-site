const debug = require('debug')('sonniesedge:routes:archive');
// ARCHIVE PAGES
// router.get('/archives', archives.getHome(req, res, next));
// router.get('/archives/:year', archives.getYear(req, res, next));
// router.get('/archives/:year/:month', archives.getYearMonth(req, res, next));
// router.get('/archives/:type', archives.getType(req, res, next));
// router.get('/archives/:type/:year', archives.getTypeYear(req, res, next));
// router.get('/archives/:type/:year/:month', archives.getTypeYearMonth(req, res, next));



module.exports = router;
